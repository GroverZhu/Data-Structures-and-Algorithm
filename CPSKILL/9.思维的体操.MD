# 思维的体操

# 目录
- [思维的体操](#思维的体操)
- [目录](#目录)
- [题目](#题目)
    - [王的对抗](#王的对抗)
    - [延伸的卡片](#延伸的卡片)
    - [公园喷水器](#公园喷水器)
    - [路痴](#路痴)
    - [EasyProblem](#EasyProblem)
    - [不屈的小蜗](#不屈的小蜗)
    - [Hailstone](#Hailstone)
    - [最长回文子串](#最长回文子串)
    - [三点顺序](#三点顺序)
    - [The Ratio of gainers to losers](#TheRatioofgainerstolosers)
# 题目



## 王的对抗
```C
# include <stdio.h>
# include <stdlib.h>

int compare(const void *a, const void *b) {
    return (*(int*)a - *(int*)b);
}

int main() {
    int total;
    int a[100];
    int b[100];
    scanf("%d", &total);
    int i = 0;
    for (; i < total; i++) {
        scanf("%d", &a[i]);
    }
    for (i = 0; i < total; i++) {
        scanf("%d", &b[i]);
    }

    qsort(a, total, sizeof(int), compare);
    qsort(b, total, sizeof(int), compare);

    int a_score = 0;
    int b_score = 0;

    for (i = 0; i < total; i++) {
        if (a[i] > b[i]) {
            a_score += 2;
            b_score += 0;
        } else if (a[i] == b[i]) {
            a_score += 1;
            b_score += 1;
        } else {
            a_score += 0;
            b_score += 2;
        }
    }

    if (a_score > b_score) {
        printf("A");
    } else if (a_score == b_score) {
        printf("=");
    } else {
        printf("B");
    }

    return 0;

}

```

## 延伸的卡片
```C
# include <stdio.h>
# include <stdlib.h>

int main() {
    float length;
    int number = 1;
    scanf("%f", &length);
    float temp = 0.0;
    while ((length - temp) > 1e-6) {
        temp += 1 / (number * 1.0 + 1.0);
        number++;
    }
    printf("%d\n", number - 1);
}
```

## 公园喷水器
```C
# include <stdio.h>
# include <stdlib.h>
# include <math.h>

int compare(const void *a, const void *b) {
    float a_float = *(float *)a;
    float b_float = *(float *)b;
    if (a_float > b_float) return 0;
    else return 1;
}

int main() {
    int total = 0;
    scanf("%d", &total);
    int i = 0;
    float spray[600] = {0};
    for (i = 0; i < total; i++) {
        scanf("%f", &spray[i]);
    }

    qsort(spray, total, sizeof(float), compare);

    float length = 20.0;
    float temp = 0.0;
    i = 0;
    while (length > temp) {
        temp += 2 * sqrt(spray[i] * spray[i] - 1);
        i++;
    }
    printf("%d\n", i);
    return 0;
}
```

## 路痴
```C
# include <stdio.h>
# include <string.h>

// 定义方向
const char directions[][6] = {
    "North",
    "South",
    "West",
    "East"
};

int main() {
    char direction[6] = "\0";
    int total = 0;
    int turn[100];
    scanf("%s %d", direction, &total);
    int i = 0;
    for (i = 0; i < total; i++) {
        scanf("%d", &turn[i]);
    }
    
    // 采用双层if-else嵌套实现方向状态机
    for (i = 0; i < total; i++) {
        if (strcmp(direction, directions[0]) == 0) {
            if (turn[i] == 0) {
                strcpy(direction, directions[2]);
            } else if (turn[i] == 1) {
                strcpy(direction, directions[3]);
            }
        } else if (strcmp(direction, directions[1]) == 0) {
            if (turn[i] == 0) {
                strcpy(direction, directions[3]);
            } else if (turn[i] == 1) {
                strcpy(direction, directions[2]);
            }
        } else if (strcmp(direction, directions[2]) == 0) {
            if (turn[i] == 0) {
                strcpy(direction, directions[1]);
            } else if (turn[i] == 1) {
                strcpy(direction, directions[0]);
            }
        } else if (strcmp(direction, directions[3]) == 0) {
            if (turn[i] == 0) {
                strcpy(direction, directions[0]);
            } else if (turn[i] == 1) {
                strcpy(direction, directions[1]);
            }
        }
    }

    printf("%s\n", direction);
    return 0;
}
```

```C
// ⚠️⚠️被CPSKILL判 Complier Error
// 使用状态机进行方向转移，双层if-else嵌套
# include <stdio.h>
# include <string.h>

// 定义方向
typedef enum {north, south, west, east} direction;

// 方向转移状态机
direction direction_machine(direction init_direction, int *turn, int times) {
    int index = 0;
    for (index = 0; index < times; index++) {
        if (init_direction == north) {
            if (turn[index] == 0) {
                init_direction = west;
            } else if (turn[index] == 1) {
                init_direction = east;
            }
        } else if (init_direction == south) {
            if (turn[index] == 0) {
                init_direction = east;
            } else if (turn[index] == 1) {
                init_direction = west;
            }
        } else if (init_direction == west) {
            if (turn[index] == 0) {
                init_direction = south;
            } else if (turn[index] == 1) {
                init_direction = north;
            }
        } else if (init_direction == east) {
            if (turn[index] == 0) {
                init_direction = north;
            } else if (turn[index] == 1) {
                init_direction = south;
            }
        }
    }
    return init_direction;
}

int main() {
    char d[6] = "\0";
    int times;
    int turn[100];
    direction init_direction = 0;

    scanf("%s %d", d, &times);
    int i = 0;
    for (i = 0; i < times; i++) {
        scanf("%d", &turn[i]);
    }
    if (strcmp(d, "North") == 0) {
        init_direction = north;
    } else if (strcmp(d, "South") == 0) {
        init_direction = south;
    } else if (strcmp(d, "West") == 0) {
        init_direction = west;
    } else if (strcmp(d, "East") == 0) {
        init_direction = east;
    }

    init_direction = direction_machine(init_direction, turn, times);

    if (init_direction == north) {
        printf("North");
    } else if (init_direction == south) {
        printf("South");
    } else if (init_direction == west) {
        printf("West");
    } else if (init_direction == east) {
        printf("East");
    }

    return 0;
}
```

## EasyProblem
```C
# include <stdio.h>
# include <math.h>

int main() {
    int number = 0;
    int count = 0;

    scanf("%d", &number);

    int i = 0;
    for (i = 2; i <= sqrt(number + 1); i++) {
        if ((number + 1) % i == 0) {
            count++;
        }
    }
    printf("%d\n", count);

    return 0;
}
```

## 不屈的小蜗
```C
# include <stdio.h>

int main() {
    int height;
    scanf("%d", &height);
    int days = (height - 10) / 5 + 1;
    printf("%d\n", days);
    return 0;
}
```

## Hailstone
```C
```

## 最长回文子串
```C
```

##  三点顺序
```C
# include <stdio.h>

int main() {
    int x1, y1, x2, y2, x3, y3;
    scanf("%d %d %d %d %d %d", &x1, &y1, &x2, &y2, &x3, &y3);
    int vector_area = (x2 - x1) * (y3 - y1) - (y2 - y1) * (x3 - x1);
    if (vector_area > 0) {
        printf("0");
    } else {
        printf("1");
    }

    return 0;
}
```

## The Ratio of gainers to losers
```C
```